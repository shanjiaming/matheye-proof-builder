{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "preLaunch",
      "type": "shell",
      "command": "bash",
      "args": ["-lc", "cd \"${workspaceFolder}\" && lake build MathEye || true; cd \"${workspaceFolder}/extension\" && npm run compile || true"],
      "problemMatcher": []
    }
  ]
}
